using System.Runtime.Versioning;

// Every operation in this assembly drives Excel through Windows COM interop, so the whole
// assembly is Windows-only. Declaring it here keeps the platform analyzer quiet for the
// Windows-only screenshot capture APIs without narrowing the target framework.
[assembly: SupportedOSPlatform("windows")]
