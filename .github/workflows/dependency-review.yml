# Dependency Review Workflow
# Scans pull requests for vulnerable dependencies before merging

name: 'Dependency Review'

on: 
  pull_request:
    branches: [ "main", "develop" ]

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          # Fail the build if vulnerabilities are found
          fail-on-severity: moderate
          
          # Fail if GPL/AGPL/LGPL licenses detected (stricter than MIT)
          license-check: true
          allow-licenses: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD, CC0-1.0, CC-BY-3.0, CC-BY-4.0, Unlicense, BlueOak-1.0.0, Python-2.0, Artistic-2.0, WTFPL, Zlib
          
          # Comment on PR with summary
          comment-summary-in-pr: always
          
          # Show vulnerabilities by severity
          warn-only: false
          
          # Only check runtime dependencies (dev deps don't ship with extension)
          fail-on-scopes: runtime
          
          # Additional configuration
          base-ref: ${{ github.event.pull_request.base.sha }}
          head-ref: ${{ github.event.pull_request.head.sha }}
          
      - name: Dependency Review Summary
        if: always()
        run: |
          echo "✅ Dependency review completed"
          echo "Review results are available in the Security tab"
