# Domain docs

This repository uses one project-wide context.

Before exploring code, read `CONTEXT.md` at the repository root and relevant decisions under `docs/adr/` when those files exist. If they do not exist, continue without warning.

Use terms defined in `CONTEXT.md`. If a proposal conflicts with a recorded decision, state that conflict rather than silently overriding it.

Expected layout:

```text
/
|-- CONTEXT.md
|-- docs/adr/
`-- src/
```
